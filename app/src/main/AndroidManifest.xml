<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agendaily2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgenDaily">
        <activity
            android:name="com.example.agendaily2.Menu"
            android:exported="true" />
        <activity
            android:name="com.example.agendaily2.Formulario"
            android:exported="true" />
        <activity
            android:name="com.example.agendaily2.Autenticacion"
            android:exported="true" />
        <activity
            android:name="com.example.agendaily2.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>